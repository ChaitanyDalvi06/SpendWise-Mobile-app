/// Central configuration for the SpendWise app.
///
/// Both frontend and backend are deployed together on the same Vercel domain.
/// After your first Vercel deploy, replace the URL below with your actual
/// Vercel URL and run: flutter build web --release, then redeploy.
///
/// Example: 'https://spendwise.vercel.app/api'
class AppConfig {
  static const String baseUrl = 'https://spendwise-five-chi.vercel.app/api';
}
