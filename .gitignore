# Environment variables - NEVER commit these
Backend/.env
Backend/node_modules/

# Flutter
Frontend/.dart_tool/
Frontend/build/
# Include the web build output so Vercel can serve it as static files
!Frontend/build/web/
Frontend/.flutter-plugins
Frontend/.flutter-plugins-dependencies
Frontend/pubspec.lock

# macOS
.DS_Store
